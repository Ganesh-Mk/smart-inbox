Classify this message using the taxonomy in the system prompt.

{{source_description}}

Return one verdict for **each** of the four categories: ICSR, PQC, MI and NOT_RELEVANT. Judge
each independently — they are not a probability distribution and they do not sum to one. A
message can genuinely be both ICSR and PQC, or both ICSR and MI.

Do not try to decide the final label set. In particular, do not reason about whether
NOT_RELEVANT should be suppressed because another label applies — that rule is applied afterwards
in code. Report what you see for each category and let the rule do its job.

Fill in the ICSR element checklist honestly, one element at a time. Each of the four minimum
criteria is judged on its own evidence; do not let the presence of one carry another. A message
with a vividly described reaction but no identifiable patient still has
`has_identifiable_patient: false`.

`reason` for each category is one sentence naming what in the source decided it — the specific
phrase, or the specific absence, not a restatement of the category definition.

--- SOURCE ---
{{source_text}}
--- END SOURCE ---
