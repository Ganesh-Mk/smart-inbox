Extract every adverse reaction described in this source.

Return one entry per distinct reaction, each with its own onset, outcome and seriousness.

- `term` is the reaction as reported. Prefer the clinical term where the source gives one
  ("angioedema"), otherwise the description as written ("swelling of the lips and tongue").
- `seriousness_criteria` contains only the criteria from the system prompt that the source
  actually supports. An empty array is correct for a reaction that is not serious. Severe
  language is not a seriousness criterion: "an agonising rash that resolved" is not serious.
- `outcome` uses the enum. UNKNOWN when the source does not say how it ended.
- Onset dates follow the date rules. Never resolve a relative date.

`narrative` is a factual paragraph, in the source language, describing the case as the document
tells it. No interpretation and no causality assessment.

If no adverse reaction is described, return an empty `reactions` array.

--- SOURCE ---
{{source_text}}
--- END SOURCE ---
