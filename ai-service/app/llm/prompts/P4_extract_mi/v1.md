Extract the medical information request from this source.

- `questions` is one entry per distinct question asked. Quote the question as written — the
  reviewer needs the enquirer's own words.
- `topic` is a short phrase: "dosing in renal impairment", "storage temperature".
- `mentions_reaction` is true when the enquirer also describes something that happened to a
  patient. That makes the message an ICSR as well, which is decided separately; record it
  honestly here rather than suppressing either the question or the reaction.
- Do not answer the questions. You are recording what was asked, not responding to it.

--- SOURCE ---
{{source_text}}
--- END SOURCE ---
